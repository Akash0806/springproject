package elevator.base;

public class ElevatorController {
	
	private Elevator  elevator;

	public ElevatorController(Elevator  elevator){
		this.elevator=elevator;
	}
	
	//shutDownElevator();
	public void start(){
		/**
		 * 1. Button press Assume from Floor button for up direction
		 * 2. Check elevator current location (assume on top and bottom floor only one button present)
		 * 3. if current location is not middel between max then start up or down
		 * 4. Assume press from zero and elevator on same location 
		 * 5. open gate 
		 * */
	}
	/*reStart();*/
}
